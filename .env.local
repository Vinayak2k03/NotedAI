# Local overrides for development.
# Ensure GOOGLE_API_KEY is set in your environment or .env (not committed).
# Select a Gemini model explicitly; the code will fall back if unavailable.
GEMINI_MODEL=gemini-1.5-pro-001
